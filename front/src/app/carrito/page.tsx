export default function CarritoPage() {
  // The visual cart modal is provided globally by `CartModal` mounted in the root layout.
  // Keep this route minimal to avoid duplicating UI when the user navigates directly.
  return null;
}
